# 📑 دليل المواصفات التقنية الشامل (Project Spec Kit)
## مشروع نظام إدارة علاقات العملاء - The Future Club (TFC) CRM

مرحباً بك! هذا المستند مخصص ليكون مرجعاً تقنياً كاملاً وتفصيلياً لأي مهندس برمجيات أو نموذج ذكاء اصطناعي (AI Model) يعمل على تطوير وصيانة هذا النظام مستقبلاً. يغطي هذا الدليل بنية التطبيق، التقنيات المستخدمة، تصميم قاعدة البيانات، نظام الأمان والصلاحيات، والأنماط البرمجية المتبعة.

---

## 1. نظرة عامة على المشروع (Project Overview)
نظام **TFC CRM** هو تطبيق ويب مصمم لشركة استشارات تمويلية وقروض عقارية وشخصية، يتيح للفريق إدارة دورة حياة العميل بداية من كونه عميلاً محتملاً (Prospect) مستورداً من إعلانات Google Sheets، إلى تحويله لعميل رسمي (Client) وتقديم طلب التمويل للبنك ومتابعته حتى القبول أو الرفض.

### التقنيات الأساسية (Tech Stack):
* **إطار العمل (Frontend Framework)**: Flutter Web (بإصدار مستقر متوافق مع الويب).
* **قاعدة البيانات والإدارة الخلفية (Backend & DB)**: Supabase (PostgreSQL).
* **إدارة حالة التطبيق (State Management)**: Flutter Riverpod (باستخدام `StateNotifierProvider` و `AsyncValue`).
* **التصميم والجماليات (Design System)**: تصميم زجاجي عصري (Glassmorphic Dark Design) يعتمد على ألوان مخصصة (`TfcColors`).

---

## 2. بنية المجلدات وهيكل الكود (Directory & Code Structure)
يتبع المشروع معمارية الطبقات المنظمة حسب الميزات (Feature-first Architecture):

```text
lib/
├── core/                  # المكونات الأساسية والمشتركة للنظام
│   ├── utils/             # دوال مساعدة (مثل فلترة رؤية البيانات للعملاء)
│   ├── widgets/           # عناصر واجهة المستخدم المشتركة (GlassCard, HoverCard)
│   ├── supabase_config.dart # إعدادات والاتصال بـ Supabase
│   └── theme.dart         # الهوية البصرية ونظام الألوان (TfcColors)
│
├── models/                # نماذج البيانات (Data Models)
│   ├── client_model.dart  # نموذج العميل الرسمي وبيانات تمويلاته
│   ├── prospect_model.dart# نموذج العميل المحتمل الوارد من جوجل شيت
│   └── employee_model.dart# نموذج بيانات الموظفين والصلاحيات
│
├── providers/             # إدارة الحالة وجلب البيانات (Riverpod Providers)
│   ├── auth_provider.dart    # إدارة تسجيل الدخول والمستخدم الحالي
│   ├── client_provider.dart  # جلب وتعديل بيانات العملاء
│   ├── prospects_provider.dart # جلب ومزامنة وتوزيع العملاء المحتملين
│   └── employees_provider.dart # إدارة قائمة الموظفين والهيكل الإداري
│
└── features/              # الميزات والصفحات المنفصلة
    ├── auth/              # شاشة تسجيل الدخول
    ├── dashboard/         # لوحة التحكم الرئيسية والتحليلات المالية
    ├── client/            # شاشات إضافة وتعديل وعرض تفاصيل العميل والخط الزمني
    ├── prospects/         # شاشة العملاء المحتملين والمزامنة من Google Sheets
    └── settings/          # إعدادات النظام ومطابقة حقول جوجل شيت
```

---

## 3. معمارية قاعدة البيانات والـ Schema (Supabase Database Design)

يعتمد التطبيق بالكامل على الجداول التالية في Supabase، مع تفعيل خاصية الـ RLS (Row Level Security) عليها:

### أ. جدول الموظفين والمسؤولين (`employees` / `profiles`)
يخزن الحسابات الإدارية وصلاحيات الموظفين:
* `id` (uuid, Primary Key) - مرتبط بجدول `auth.users` في Supabase.
* `full_name` (text) - الاسم الكامل للموظف.
* `email` (text) - البريد الإلكتروني.
* `role` (text) - صلاحية المستخدم: `admin` (مدير كامل)، `manager` (مشرف قسم)، `employee` (موظف مبيعات)، `bank_employee` (ممثل البنك).
* `manager_id` (uuid) - معرف المدير المباشر لبناء الهيكل الإداري والمتابعة.

### ب. جدول العملاء الأساسي (`clients`)
يخزن تفاصيل العملاء المتقدمين لطلبات التمويل:
* `id` (text, Primary Key).
* `full_name` (text) - الاسم الكامل.
* `phone_number` (text) - الهاتف.
* `national_id` (text) - الرقم القومي.
* `credit_score` (integer) - التقييم الائتماني (I-Score).
* `requested_amount` (numeric) - مبلغ التمويل المطلوب.
* `status` (text) - حالة الطلب الحالية:
  `pending`, `iscore_inquiry`, `preparing_documents`, `under_review`, `at_bank`, `approved`, `rejected`.
* `representative_name` (text) - اسم الموظف المسؤول عن العميل حالياً.
* `created_by` (uuid) - الموظف الذي أنشأ الملف.

### ج. جدول العملاء المحتملين (`prospects`)
يخزن العملاء المجلوبين من حملات التسويق (Google Sheets):
* `id` (uuid, Primary Key, Auto-generated).
* `full_name` (text) - الاسم الكامل.
* `phone_number` (text).
* `assigned_to_id` (text) - معرّف الموظف المسند إليه المتابعة.
* `assigned_to_name` (text) - اسم الموظف المسند إليه المتابعة.
* `status` (text) - حالة المتابعة: `pending`, `contacted`, `converted`, `rejected`.
* `is_converted` (boolean) - هل تم تحويله لعميل رسمي بنجاح أم لا.
* `raw_data` (jsonb) - لحفظ كامل بيانات الصف المستورد من شيت جوجل بشكل مرن.

---

## 4. نظام الصلاحيات ومستوى الأمان (Row Level Security & Scope)

أهم قاعدة في هذا النظام هي **أمن وسرية البيانات** بناءً على الدور الوظيفي (Role) والموقع في الهيكل الإداري (Manager-Subordinate Hierarchy):

### قواعد فلترة البيانات البرمجية (`ClientVisibilityHelper`):
1. **المدير العام (`admin`)**: يرى كافة البيانات والتحليلات والعملاء والعملاء المحتملين دون قيود.
2. **المشرف (`manager`)**: يرى عملائه المباشرين بالإضافة إلى جميع العملاء المسندين للموظفين الذين يعملون تحت إدارته (Subordinates).
3. **الموظف المالي (`employee`)**: يرى فقط العملاء المسندين إليه بشكل مباشر، ولا يمكنه الاطلاع على عملاء زملائه.
4. **موظف البنك (`bank_employee`)**: يرى فقط العملاء الذين وصلوا إلى حالة "في البنك" (`at_bank`) والموجهين لبنكه تحديداً.

> [!IMPORTANT]
> عند كتابة أي استعلامات أو شاشات جديدة، يجب دائماً تمرير القائمة المسترجعة عبر `ClientVisibilityHelper.filterClients` أو `ClientVisibilityHelper.filterProspects` لضمان عدم تسريب أي بيانات.

---

## 5. الأنماط البرمجية المتبعة وقواعد التطوير (Development Standard & Rules)

إذا كنت نموذج ذكاء اصطناعي أو مطوراً جديداً، يرجى الالتزام بالقواعد التالية أثناء العمل على هذا الكود:

### أ. نمط الاتصال بقاعدة البيانات (DB-First Pattern)
* **القاعدة الذهبية**: لا تقم أبداً بتحديث الحالة محلياً في الـ UI أو الـ Provider قبل نجاح العملية في قاعدة بيانات Supabase.
* **خطوات الحفظ الصحيحة**:
  1. أرسل الطلب إلى Supabase باستخدام `SupabaseConfig.client`.
  2. انتظر الاستجابة (`await`).
  3. احصل على السجل الجديد كاملاً بالـ ID الذي أنشأته قاعدة البيانات (خاصة في الـ `insert`).
  4. حدّث الـ State المحلي باستخدام Riverpod بالبيانات الحقيقية الراجعة من قاعدة البيانات.
  5. في حالة حدوث استثناء (Exception)، قم بإرجاع `false` وتمرير رسالة الخطأ للـ UI ولا تقم بتحديث الواجهة محلياً.

### ب. الهوية البصرية والتصميم (Glassmorphism Dark UI)
* تجنب استخدام ألوان عشوائية أو سطوع فاقع.
* استخدم دائماً بطاقة `GlassCard` الجاهزة لإعطاء الواجهة مظهراً زجاجياً متناسقاً.
* استخدم نظام ألوان التطبيق من الكلاس `TfcColors` الموحد:
  - اللون الرئيسي: `TfcColors.primary` (الذهبي/الأصفر النيون الهادئ).
  - لون الخلفيات: `TfcColors.background` (الداكن العميق).
  - الألوان الثانوية والنجاح والتحذير معرفة بالكامل في `theme.dart`.

### ج. التعامل مع الـ Web وحفظ البيانات المؤقتة
* بعض الميزات (مثل إعدادات مزامنة Google Sheets) تدعم العمل بنمط الهجين (Hybrid): تُحفظ في الـ DB وإذا لم تكن متوفرة تُحفظ محلياً باستخدام `SharedPreferences` كبديل احتياطي لضمان استمرارية التشغيل.

---
💡 *هذا المستند تم تحديثه تلقائياً ليعكس بنية الكود الحالية للنظام وجاهز للاستخدام الفوري كمستند تغذية للنماذج الذكية.*
